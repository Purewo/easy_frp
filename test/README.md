# Manual Test Fixtures

This directory contains local projects used to verify port exposure against realistic services.

- `path2agi` is a static site fixture.
- `cyberstream` is a Vite/React fixture.
- `runtime` is ignored and may contain generated client data, frpc configs, logs, and temporary local state.

These fixtures are not product code. Keep dependencies, build output, and runtime state out of git.
